{
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "jest",
    "doc": "typedoc --out doc src/index.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typedoc": "^0.24.8",
    "typescript": "^5.1.3"
  }
}
